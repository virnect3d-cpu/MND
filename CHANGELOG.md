# Changelog

이 레포(Unity 프로젝트 전체)의 변경 기록이다.
도시 에셋 패키지 자체의 기록은 `unity/DRON/Assets/yeouido63/CHANGELOG.md` 에 따로 있다.

## [Unreleased]

### 성능

- 안 쓰는 패키지 5 개 제거 — 도메인 리로드가 평균 6.5 초였고,
  그중 `FinalizeReload` 가 81%(5298ms)를 먹고 있었다.
  쓰지도 않는 패키지들이 리로드마다 초기화 코드를 돌린 게 원인이다.
  `visualscripting`(어셈블리 8 개), `collab-proxy`(3 개, git 을 쓰는데 Plastic),
  `timeline`, `multiplayer.center`, `ai.navigation` — 전부 사용 흔적 0 을
  확인하고 뺐다. 의존성 44 → 38 개.
  `inputsystem` 은 `activeInputHandler` 가 Both 라 남겼다.
- URP 템플릿 잔재(`Assets/TutorialInfo`, `Assets/Readme.asset`) 제거.
  `ReadmeEditor` 가 프로젝트에서 유일하게 가드 없는 `[InitializeOnLoad]` 로
  매 리로드마다 콜백을 예약하고 있었다.
- 먼지 파티클 20% 감축 — MID 6000 → 4800, GRIT 60 → 48.
  HAZE(25)는 알파가 겹치는 장수에 반비례해 역산한 값이라 그대로 뒀다.

### 구름 (`Assets/VolumetricClouds`, 로컬 패치)

- **레이 시작점 지터 복구.** URP 포팅 과정에서 `* stepS` 가 누락돼
  지터 폭이 스텝 간격의 2.4% 밖에 안 됐다. 시작점이 안 흩어지니 모든
  픽셀이 같은 평면에서 밀도를 떠서 계단과 띠가 남았다.
- **프레임마다 노이즈가 안 바뀌던 문제.** `_Seed += 1.0` 은 CBUFFER
  상수라 효과가 없었고, `unity_DeltaTime.y` 기반 시드는 프레임률이
  안정되면 거의 같은 값이었다. 고정 패턴 + 재투영 누적이 겹쳐
  플레이 모드에서 덜컥거림이 더 도드라졌다.
- **화이트노이즈 → R2 저불일치 수열.** 업스트림이 쓰던 Jenkins 해시는
  이름과 달리 블루노이즈가 아니다. 공간 균일성 3.1 배, 시간 수렴
  32 프레임 기준 3.8 배 개선. 연산은 오히려 더 싸고 텍스처 샘플은 0 이다.
- **`relativeStepSize` 제거.** `saturate(int)` 라 첫 스텝만 적용되던
  코드였고, 지터 복구 후에는 같은 난수를 두 번 먹어 효과를 깎았다.
  HDRP 원본도 여기서 균일 전진한다.
- 구름 위치 누적값 `half` → `float`. 느린 드리프트(0.025)에서 오프셋이
  멈췄다 튀는 현상을 없앴다.
- 스텝 간격을 정하는 `_NumPrimarySteps`, `_MaxStepSize` 도 `float` 로.
  나머지 `half` 27 개는 전수 점검 후 그대로 뒀다 — 0~1 계수·색·SH 라
  올리면 레지스터만 낭비다.

### 수정

- 구름 아지랑이 제거 — 원인은 구름 셰이더가 아니라 URP 카메라 디더링이었다.

### 정리

- 역할이 끝난 진단 스크립트 삭제, 리로드 플래그·경로 처리를 공통화.
  같은 7 줄이 28 곳에 복붙돼 있었고 예외 처리만 제각각이었다.

## [0.1.0] - 2026-09-14

여의도 63 씬의 첫 정리 시점. 강물·하늘 큐브맵·포스트 FX·63빌딩 옥상 모델이
들어가고, 전체 코드 리뷰로 값 소유권 충돌과 죽은 참조를 정리했다.

## 태그

| 태그 | 의미 |
|---|---|
| `v0.1.0` | 위 참조 |
| `prelfs/main`, `prelfs/feat` | Git LFS 도입 직전 시점 |
